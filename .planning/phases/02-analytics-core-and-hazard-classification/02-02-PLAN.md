---
phase: 02-analytics-core-and-hazard-classification
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/analytics/sql/hazard_classification.sql
  - src/analytics/classify_hazard.py
  - src/analytics/thresholds.py
autonomous: true
requirements:
  - ANL-02
user_setup: []
must_haves:
  truths:
    - Hazard classification uses the exact PHA thresholds from requirements.
    - Each typed NEO row receives a deterministic boolean hazard flag.
    - Classification can be rerun without schema drift or threshold ambiguity.
  artifacts:
    - src/analytics/sql/hazard_classification.sql
    - src/analytics/classify_hazard.py
    - src/analytics/thresholds.py
  key_links:
    - classify_hazard.py imports threshold constants and executes hazard_classification.sql.
    - hazard_classification.sql reads typed table produced by 02-01 and writes hazard-enriched table.
---

<objective>
Implement deterministic PHA classification in DuckDB using explicit threshold constants.

Purpose: Translate ANL-02 logic into executable SQL/Python so hazard decisions are explainable and reproducible.
Output: Threshold constants module, hazard SQL, and runnable classification script.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-analytics-core-and-hazard-classification/02-01-PLAN.md
@src/analytics/transform_raw_to_typed.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define hazard threshold constants</name>
  <files>src/analytics/thresholds.py</files>
  <read_first>.planning/REQUIREMENTS.md</read_first>
  <action>Create constants for PHA_MISS_DISTANCE_AU_MAX = 0.05 and PHA_DIAMETER_M_MIN = 140.0 with clear exported names. Add helper function describe_hazard_rule() returning the exact rule string for logging and audit traceability.</action>
  <verify>grep -E "0.05|140|PHA_MISS_DISTANCE_AU_MAX|PHA_DIAMETER_M_MIN|describe_hazard_rule" src/analytics/thresholds.py</verify>
  <acceptance_criteria>
    - thresholds.py exports miss-distance and diameter constants with exact requirement values
    - helper function returns the same threshold logic statement used by scripts
  </acceptance_criteria>
  <done>Threshold source of truth is centralized and deterministic.</done>
</task>

<task type="auto">
  <name>Task 2: Encode hazard flag SQL against typed table</name>
  <files>src/analytics/sql/hazard_classification.sql</files>
  <read_first>src/analytics/sql/raw_to_typed.sql, src/analytics/thresholds.py</read_first>
  <action>Create SQL that reads neo_typed_staging (from 02-01) and materializes neo_hazard_classified table including a boolean column is_potentially_hazardous. Flag true only when miss_distance_au &lt; 0.05 AND estimated_diameter_m &gt; 140.0. Preserve key columns needed for downstream dashboard queries.</action>
  <verify>grep -E "neo_hazard_classified|is_potentially_hazardous|miss_distance_au < 0.05|estimated_diameter_m > 140" src/analytics/sql/hazard_classification.sql</verify>
  <acceptance_criteria>
    - SQL defines output table neo_hazard_classified
    - hazard boolean expression exactly matches ANL-02 thresholds and operators
    - output keeps object_id, object_name, close_approach_date, velocity, distance, diameter fields
  </acceptance_criteria>
  <done>PHA logic is encoded as executable SQL and is requirement-aligned.</done>
</task>

<task type="auto">
  <name>Task 3: Implement hazard classification runner</name>
  <files>src/analytics/classify_hazard.py</files>
  <read_first>src/analytics/thresholds.py, src/analytics/sql/hazard_classification.sql</read_first>
  <action>Implement script to connect to neo_analytics.db, execute hazard_classification.sql, and log counts for total rows and flagged rows. Require typed source table existence before execution and fail fast with clear error if missing. Add python -m entrypoint.</action>
  <verify>grep -E "duckdb.connect|neo_analytics.db|neo_hazard_classified|flagged|if __name__ == '__main__'" src/analytics/classify_hazard.py</verify>
  <acceptance_criteria>
    - script executes hazard SQL into neo_analytics.db
    - script logs deterministic totals and flagged PHA counts
    - script fails clearly when neo_typed_staging does not exist
  </acceptance_criteria>
  <done>Hazard classification can be executed repeatably with auditable threshold usage.</done>
</task>

</tasks>

<threat_model>
- Asset: Trustworthy hazard label used for risk-facing UI.
- Threat: Threshold drift or implicit casts produce false positives/negatives.
- Mitigation: Central constants, explicit SQL condition, and runner logs with rule string.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python -m src.analytics.classify_hazard` exits successfully after 02-01
- [ ] `python -c "import duckdb; con=duckdb.connect('neo_analytics.db'); print(con.execute('select count(*) from neo_hazard_classified').fetchone()[0])"` runs without errors
- [ ] `python -c "import duckdb; con=duckdb.connect('neo_analytics.db'); print(con.execute('select count(*) from neo_hazard_classified where is_potentially_hazardous').fetchone()[0])"` returns numeric output
</verification>

<success_criteria>
- ANL-02 satisfied with exact threshold-based PHA flagging
- Hazard flags are queryable in neo_hazard_classified table
- Classification is deterministic across reruns on unchanged input
</success_criteria>

<output>
After completion, create `.planning/phases/02-analytics-core-and-hazard-classification/02-02-SUMMARY.md`
</output>
