---
phase: 02-analytics-core-and-hazard-classification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analytics/__init__.py
  - src/analytics/transform_raw_to_typed.py
  - src/analytics/sql/raw_to_typed.sql
autonomous: true
requirements:
  - RAW-02
  - RAW-03
  - ANL-01
user_setup: []
must_haves:
  truths:
    - Raw parquet rows can be loaded into deterministic typed DuckDB tables.
    - Duplicate records for identical ingestion windows are removed before analytics use.
    - Typed analytics schema includes core numeric/date fields needed by hazard logic.
  artifacts:
    - src/analytics/__init__.py
    - src/analytics/transform_raw_to_typed.py
    - src/analytics/sql/raw_to_typed.sql
  key_links:
    - transform_raw_to_typed.py executes raw_to_typed.sql against local raw parquet partitions.
    - raw_to_typed.sql writes a typed table with deterministic casts and dedupe logic.
---

<objective>
Establish deterministic raw-to-typed analytics transformation using DuckDB SQL-first patterns.

Purpose: Convert Phase 1 raw parquet into a stable typed table that downstream hazard and serving logic can trust.
Output: Analytics package scaffold, SQL transform, and executable transform script.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@src/storage/consume_stream_to_parquet.py
@src/storage/parquet_layout.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create analytics module skeleton and SQL contract</name>
  <files>src/analytics/__init__.py, src/analytics/sql/raw_to_typed.sql</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</read_first>
  <action>Create analytics package and SQL file defining deterministic typed staging table (for example neo_typed_staging) sourced from data/raw/year=*/month=*/day=*/neo_data.parquet. Include explicit casts for object_id, object_name, close_approach_date, miss_distance_au, estimated_diameter_m, relative_velocity_km_s, and ingestion timestamp fields. Include row_number-based dedupe keyed by event_id keeping latest fetched_at_utc row.</action>
  <verify>grep -E "neo_typed_staging|read_parquet|cast|row_number|event_id" src/analytics/sql/raw_to_typed.sql</verify>
  <acceptance_criteria>
    - analytics package directory and __init__.py exist
    - raw_to_typed.sql defines a typed staging table creation step
    - SQL includes deterministic dedupe using event_id and fetched_at_utc ordering
  </acceptance_criteria>
  <done>Typed transformation contract is explicit and versionable in SQL.</done>
</task>

<task type="auto">
  <name>Task 2: Implement raw-to-typed transformation runner</name>
  <files>src/analytics/transform_raw_to_typed.py</files>
  <read_first>src/common/config.py, src/analytics/sql/raw_to_typed.sql</read_first>
  <action>Implement a script that opens DuckDB connection, executes raw_to_typed.sql, and materializes output into local neo_analytics.db. Use explicit transaction boundaries and deterministic run metadata logging (input parquet glob, row counts, output table name). Add CLI entrypoint so the script can be run as python -m src.analytics.transform_raw_to_typed.</action>
  <verify>grep -E "duckdb.connect|neo_analytics.db|raw_to_typed.sql|if __name__ == '__main__'" src/analytics/transform_raw_to_typed.py</verify>
  <acceptance_criteria>
    - script opens DuckDB file neo_analytics.db and executes SQL transform
    - script logs transformed row count and output table name
    - module is runnable with python -m src.analytics.transform_raw_to_typed
  </acceptance_criteria>
  <done>Raw parquet can be reproducibly transformed into typed analytics data.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic transform smoke command</name>
  <files>README.md</files>
  <read_first>README.md, src/analytics/transform_raw_to_typed.py</read_first>
  <action>Add a short "Phase 2 transform smoke check" section documenting command to run transform and query typed table count with duckdb CLI or python one-liner. Include expected non-negative row count and database file path neo_analytics.db.</action>
  <verify>grep -E "Phase 2 transform smoke check|neo_analytics.db|transform_raw_to_typed" README.md</verify>
  <acceptance_criteria>
    - README includes exact transform command
    - README includes simple typed-row verification command
    - README states neo_analytics.db as output artifact
  </acceptance_criteria>
  <done>Operator can validate raw-to-typed transformation with one command sequence.</done>
</task>

</tasks>

<threat_model>
- Asset: Correctness of typed analytical fields used for hazard decisions.
- Threat: Non-deterministic casting/deduping causes inconsistent analytics outputs.
- Mitigation: SQL-first transform with explicit casts, stable dedupe ordering, and repeatable DB target.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python -m src.analytics.transform_raw_to_typed` exits successfully
- [ ] `test -f neo_analytics.db` returns success
- [ ] Typed staging table row count query returns without errors
</verification>

<success_criteria>
- RAW-02 satisfied by typed core field projection
- RAW-03 satisfied by deterministic dedupe for duplicate ingestion windows
- ANL-01 foundation established with DuckDB typed transform table
</success_criteria>

<output>
After completion, create `.planning/phases/02-analytics-core-and-hazard-classification/02-01-SUMMARY.md`
</output>
