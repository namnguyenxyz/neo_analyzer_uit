---
phase: 02-analytics-core-and-hazard-classification
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/analytics/build_analytics_db.py
  - src/analytics/sql/serving_views.sql
  - README.md
autonomous: true
requirements:
  - ANL-03
user_setup: []
must_haves:
  truths:
    - A single local DuckDB file neo_analytics.db persists typed and hazard-serving objects.
    - Query-ready serving objects expose counts and nearest-approach analytics for downstream dashboard use.
    - Rebuild command produces the same schema layout deterministically from current raw input.
  artifacts:
    - src/analytics/build_analytics_db.py
    - src/analytics/sql/serving_views.sql
    - README.md
  key_links:
    - build_analytics_db.py orchestrates transform (02-01), classify (02-02), and serving view SQL.
    - README commands verify neo_analytics.db existence and basic queryability.
---

<objective>
Finalize persisted analytics serving layer and queryability checks for neo_analytics.db.

Purpose: Deliver ANL-03 and phase success criteria that require a stable local analytics database for dashboard consumption.
Output: End-to-end analytics DB builder script, serving views SQL, and run/verify documentation.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-analytics-core-and-hazard-classification/02-01-PLAN.md
@.planning/phases/02-analytics-core-and-hazard-classification/02-02-PLAN.md
@src/analytics/transform_raw_to_typed.py
@src/analytics/classify_hazard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement serving views SQL for dashboard-ready reads</name>
  <files>src/analytics/sql/serving_views.sql</files>
  <read_first>src/analytics/sql/hazard_classification.sql, .planning/ROADMAP.md</read_first>
  <action>Create SQL to materialize or replace serving objects derived from neo_hazard_classified, including at minimum: v_neo_latest (latest typed/hazard rows), v_pha_counts (total and hazardous counts), and v_closest_approach_today. Ensure views avoid nondeterministic ordering by using explicit ORDER BY where needed.</action>
  <verify>grep -E "v_neo_latest|v_pha_counts|v_closest_approach_today|create (or replace )?view" -i src/analytics/sql/serving_views.sql</verify>
  <acceptance_criteria>
    - serving_views.sql defines three serving views for latest rows, hazard counts, and closest approach
    - SQL references neo_hazard_classified as source
    - deterministic ordering is explicitly defined for nearest/closest selections
  </acceptance_criteria>
  <done>Dashboard-facing query contracts exist in stable SQL objects.</done>
</task>

<task type="auto">
  <name>Task 2: Build end-to-end analytics DB orchestrator</name>
  <files>src/analytics/build_analytics_db.py</files>
  <read_first>src/analytics/transform_raw_to_typed.py, src/analytics/classify_hazard.py, src/analytics/sql/serving_views.sql</read_first>
  <action>Implement orchestrator that executes transform_raw_to_typed then classify_hazard then serving_views.sql in sequence against neo_analytics.db. Add optional --rebuild flag to drop/recreate derived tables/views deterministically. Log each phase step with row/view counts and exit non-zero on any step failure.</action>
  <verify>grep -E "transform_raw_to_typed|classify_hazard|serving_views.sql|--rebuild|neo_analytics.db|if __name__ == '__main__'" src/analytics/build_analytics_db.py</verify>
  <acceptance_criteria>
    - build script runs all analytics stages in deterministic order
    - build script supports rebuild mode and fails fast on stage errors
    - build script reports completion with database path and key object counts
  </acceptance_criteria>
  <done>Single command can produce a queryable analytics database artifact.</done>
</task>

<task type="auto">
  <name>Task 3: Document Phase 2 end-to-end build and query checks</name>
  <files>README.md</files>
  <read_first>README.md, src/analytics/build_analytics_db.py</read_first>
  <action>Add "Phase 2 analytics build" section with exact commands to run builder and execute three verification queries against neo_analytics.db (total rows, hazardous rows, closest approach today). Include expected condition checks (queries succeed and return scalar values).</action>
  <verify>grep -E "Phase 2 analytics build|neo_analytics.db|v_pha_counts|v_closest_approach_today|build_analytics_db" README.md</verify>
  <acceptance_criteria>
    - README includes one-command analytics DB build invocation
    - README includes query checks for total, hazardous, and closest-approach outputs
    - README states neo_analytics.db as Phase 2 persisted serving artifact
  </acceptance_criteria>
  <done>Operator can prove analytics DB persistence and queryability with documented commands.</done>
</task>

</tasks>

<threat_model>
- Asset: Availability and integrity of local analytics serving database.
- Threat: Partial pipeline execution leaves stale/incomplete serving objects.
- Mitigation: Ordered orchestration, explicit rebuild path, and non-zero exit on failed stage.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python -m src.analytics.build_analytics_db --rebuild` exits successfully
- [ ] `test -f neo_analytics.db` returns success
- [ ] `python -c "import duckdb; con=duckdb.connect('neo_analytics.db'); print(con.execute('select * from v_pha_counts').fetchall())"` returns rows
- [ ] `python -c "import duckdb; con=duckdb.connect('neo_analytics.db'); print(con.execute('select * from v_closest_approach_today').fetchall())"` returns rows or empty set without error
</verification>

<success_criteria>
- ANL-03 satisfied via persisted, queryable neo_analytics.db
- Phase 2 success criteria met for typed tables, hazard flags, and serving queryability
- README documents reproducible local build-and-verify path
</success_criteria>

<output>
After completion, create `.planning/phases/02-analytics-core-and-hazard-classification/02-03-SUMMARY.md`
</output>
