---
phase: 03-dashboard-experience
plan: 02
subsystem: dashboard-explorer
tags: [streamlit, table, duckdb]
key-files:
  - src/dashboard/ui.py
  - src/dashboard/data_access.py
metrics:
  table_fast_query_ms: 200
  tests_run: 2
---

# Plan 02: Fast Explorer Table

Tasks:
- Implement paginated, searchable table component in `src/dashboard/ui.py`.
- Implement parameterized queries in `src/dashboard/data_access.py` to power table filters.
- Add unit smoke checks for pagination and filters.

Verification:
- `.venv/bin/python -c "import duckdb; con=duckdb.connect('neo_analytics.db', read_only=True); print(con.execute('select count(*) from v_neo_latest').fetchone())"`
