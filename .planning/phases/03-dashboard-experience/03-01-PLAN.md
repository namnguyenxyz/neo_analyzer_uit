---
phase: 03-dashboard-experience
plan: 01
subsystem: dashboard-kpis
tags: [streamlit, kpi, duckdb]
key-files:
  - src/dashboard/app.py
  - src/dashboard/data_access.py
metrics:
  cards_done: 3
  tests_run: 2
---

# Plan 01: KPI Cards and Read-only DB Access

Tasks:
- Implement `src/dashboard/data_access.py` with read-only DuckDB helpers.
- Add KPI components to `src/dashboard/app.py`: total NEOs, total PHAs, closest approach today.
- Add smoke checks to README for `streamlit run` and sample queries.

Verification:
- `.venv/bin/python -c "import duckdb; con=duckdb.connect('neo_analytics.db', read_only=True); print(con.execute('select count(*) from neo_hazard_classified').fetchone())"`
