---
phase: 04-hardening-and-delivery
plan: 01
subsystem: dependency-hardening
tags: [requirements, versions, env]
key-files:
  - requirements.txt
metrics:
  pins_verified: 7
  tests_run: 2
---

# Plan 01: Dependency Manifest Audit

Tasks:
- Verify `requirements.txt` pins exact versions for all runtime packages used by ingestion, analytics, and dashboard code.
- Check that the local `.venv` resolves the same package set used by the smoke tests.
- Confirm no unpinned runtime dependency is introduced by the current repository state.

Verification:
- `.venv/bin/python - << 'PY'` to print installed versions for `duckdb`, `pandas`, `plotly`, `pyarrow`, `redis`, `requests`, and `streamlit`
- `python -c "from pathlib import Path; print(Path('requirements.txt').read_text())"`
