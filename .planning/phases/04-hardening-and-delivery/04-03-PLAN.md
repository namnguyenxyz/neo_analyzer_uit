---
phase: 04-hardening-and-delivery
plan: 03
subsystem: release-validation
tags: [smoke-test, end-to-end, delivery]
key-files:
  - src/ingestion/fetch_nasa_neos.py
  - src/analytics/build_analytics_db.py
  - src/dashboard/app.py
  - README.md
metrics:
  checks_run: 4
  tests_run: 3
---

# Plan 03: Full Release Smoke and Sign-off

Tasks:
- Run the end-to-end local smoke path from ingestion output through the dashboard surface using the current sample data.
- Confirm the analytics build still generates `neo_analytics.db` with the serving views required by the dashboard.
- Validate that the dashboard opens against the local DB without importing any heavy runtime layer.

Verification:
- `python -m src.analytics.build_analytics_db --rebuild`
- `streamlit run src/dashboard/app.py`
- Query the DuckDB file for `v_neo_latest`, `v_pha_counts`, and `v_closest_approach_today`
