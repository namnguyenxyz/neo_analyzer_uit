---
phase: 01-ingestion-baseline-and-raw-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - src/common/config.py
  - src/common/logging_utils.py
autonomous: true
requirements:
  - OPS-01
user_setup: []
must_haves:
  truths:
    - Redis service for NEO stream is runnable with explicit memory constraints.
    - Pipeline scripts can resolve redis host/port/stream from a single config module.
  artifacts:
    - docker-compose.yml
    - src/common/config.py
    - src/common/logging_utils.py
  key_links:
    - Producer and consumer import shared config and logging utilities.
---

<objective>
Establish Phase 1 operational foundation for Redis-backed streaming and shared runtime configuration.

Purpose: Create a reliable base so ingestion and storage scripts can run consistently across environments.
Output: Minimal Redis compose service with memory guardrails and shared Python config/logging modules.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define minimal Redis runtime service</name>
  <files>docker-compose.yml</files>
  <read_first>README.md</read_first>
  <action>Create docker compose service named redis using image redis:7-alpine, bind container port 6379 to host 6379, add restart: unless-stopped, and enforce memory cap with command redis-server --appendonly no --maxmemory 256mb --maxmemory-policy allkeys-lru. Include container name neo-redis.</action>
  <verify>grep -E "redis:7-alpine|--maxmemory 256mb|allkeys-lru|neo-redis" docker-compose.yml</verify>
  <acceptance_criteria>
    - docker-compose.yml contains service key redis with image redis:7-alpine
    - docker-compose.yml contains command redis-server --appendonly no --maxmemory 256mb --maxmemory-policy allkeys-lru
    - docker-compose.yml maps port 6379:6379
  </acceptance_criteria>
  <done>Redis compose baseline exists and encodes memory limits explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Add shared pipeline configuration module</name>
  <files>src/common/config.py</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</read_first>
  <action>Implement constants and helper functions for REDIS_HOST, REDIS_PORT, REDIS_STREAM_NAME default neo:stream, NASA API base URL, polling interval seconds, and RAW_DATA_ROOT path. Read env vars with defaults and return a normalized dict via get_runtime_config().</action>
  <verify>grep -E "REDIS_STREAM_NAME|neo:stream|get_runtime_config|NASA" src/common/config.py</verify>
  <acceptance_criteria>
    - src/common/config.py defines REDIS_STREAM_NAME with default neo:stream
    - src/common/config.py exports get_runtime_config function
    - get_runtime_config includes redis, nasa_api, and storage settings keys
  </acceptance_criteria>
  <done>Producer and consumer can share one source of runtime truth.</done>
</task>

<task type="auto">
  <name>Task 3: Add reusable structured logging helper</name>
  <files>src/common/logging_utils.py</files>
  <read_first>src/common/config.py</read_first>
  <action>Create get_logger(name) helper that configures INFO-level stream handler with timestamp, level, module name, and message format. Ensure repeated calls do not duplicate handlers by checking logger.handlers before adding.</action>
  <verify>grep -E "def get_logger|logger.handlers|INFO" src/common/logging_utils.py</verify>
  <acceptance_criteria>
    - src/common/logging_utils.py defines get_logger(name)
    - get_logger avoids duplicate handlers when called multiple times
    - log format includes timestamp and level tokens
  </acceptance_criteria>
  <done>All phase scripts can emit consistent low-overhead logs.</done>
</task>

</tasks>

<threat_model>
- Asset: Raw NEO payload integrity and ingestion continuity.
- Threat: Redis memory exhaustion causes data loss.
- Mitigation: Hard memory cap and eviction policy defined in compose; bounded message size in producer.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `docker compose config` validates compose syntax
- [ ] Shared config module imports without errors
- [ ] Logging helper emits one handler per logger name
</verification>

<success_criteria>
- Redis compose baseline is runnable and memory bounded
- Shared config/logging modules exist and are importable
- No heavy dependencies introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-ingestion-baseline-and-raw-capture/01-01-SUMMARY.md`
</output>
