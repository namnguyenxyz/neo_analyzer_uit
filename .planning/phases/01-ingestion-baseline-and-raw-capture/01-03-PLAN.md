---
phase: 01-ingestion-baseline-and-raw-capture
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/storage/consume_stream_to_parquet.py
  - src/storage/parquet_layout.py
autonomous: true
requirements:
  - RAW-01
user_setup: []
must_haves:
  truths:
    - Consumer drains redis stream events and appends normalized records into daily partitioned Parquet files.
    - Write path avoids duplicate rows for repeated event ids in a processing batch.
  artifacts:
    - src/storage/consume_stream_to_parquet.py
    - src/storage/parquet_layout.py
  key_links:
    - consumer script maps stream payloads to parquet schema and partition directory convention.
---

<objective>
Implement raw storage consumer that persists stream events into partitioned Parquet files.

Purpose: Complete Phase 1 end-to-end path from API ingestion to durable local raw lake.
Output: Redis consumer loop and parquet partition writer utilities.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/common/config.py
@src/ingestion/publish_stream.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement parquet partition helper module</name>
  <files>src/storage/parquet_layout.py</files>
  <read_first>src/common/config.py, .planning/REQUIREMENTS.md</read_first>
  <action>Create helper functions resolve_partition_path(base_dir, approach_date) and ensure_partition_dir(path) that produce directory layout data/raw/year=YYYY/month=MM/day=DD/. Add build_parquet_file_path(base_dir, approach_date) returning neo_data.parquet in that partition.</action>
  <verify>grep -E "year=|month=|day=|neo_data.parquet|resolve_partition_path" src/storage/parquet_layout.py</verify>
  <acceptance_criteria>
    - parquet_layout.py returns partition paths with year=YYYY/month=MM/day=DD segments
    - parquet_layout.py returns neo_data.parquet as target file name
    - ensure_partition_dir creates missing partition folders idempotently
  </acceptance_criteria>
  <done>Raw writer has deterministic filesystem layout utilities.</done>
</task>

<task type="auto">
  <name>Task 2: Consume redis stream and write deduplicated parquet batches</name>
  <files>src/storage/consume_stream_to_parquet.py</files>
  <read_first>src/storage/parquet_layout.py, src/common/config.py, src/ingestion/publish_stream.py</read_first>
  <action>Implement consume_stream_forever() using XREADGROUP or XREAD against neo:stream with bounded batch size. Parse payload_json into normalized records, dedupe in-batch by event_id, group by approach_date partition, and append to parquet using pandas+pyarrow. Acknowledge stream entries only after successful write.</action>
  <verify>grep -E "XREAD|XREADGROUP|event_id|to_parquet|payload_json|ack" src/storage/consume_stream_to_parquet.py</verify>
  <acceptance_criteria>
    - consumer script reads from neo:stream using redis stream read command
    - script deduplicates records by event_id before write
    - script writes parquet files under data/raw/year=*/month=*/day=*/neo_data.parquet
    - stream acknowledgements occur after successful parquet write
  </acceptance_criteria>
  <done>Consumer reliably persists raw events with duplicate protection.</done>
</task>

<task type="auto">
  <name>Task 3: Add end-to-end raw capture smoke script section</name>
  <files>README.md</files>
  <read_first>README.md, src/storage/consume_stream_to_parquet.py</read_first>
  <action>Add exact run sequence for Phase 1: start Redis, run producer, run consumer once/loop, then verify parquet output with find data/raw -name neo_data.parquet. Include expected directory pattern year=YYYY/month=MM/day=DD.</action>
  <verify>grep -E "find data/raw -name neo_data.parquet|year=YYYY/month=MM/day=DD" README.md</verify>
  <acceptance_criteria>
    - README.md contains full producer->consumer smoke run order
    - README.md includes find command for parquet verification
    - README.md documents expected partition path format
  </acceptance_criteria>
  <done>Operator can verify end-to-end raw capture from stream to parquet.</done>
</task>

</tasks>

<threat_model>
- Asset: Durability and correctness of raw captured data.
- Threat: Acknowledge-before-write causes irrecoverable data loss.
- Mitigation: Acknowledge stream events only after parquet append succeeds; retain failed batch logs for replay.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] Consumer writes at least one partitioned parquet file from stream events
- [ ] Duplicate event ids in a batch are collapsed to one persisted record
- [ ] Redis entries are acknowledged only after successful writes
</verification>

<success_criteria>
- Raw records are persisted in date-partitioned parquet layout
- Consumer behavior is resilient to retry/replay scenarios
- Phase 1 requirement RAW-01 is fully implementable from this plan
</success_criteria>

<output>
After completion, create `.planning/phases/01-ingestion-baseline-and-raw-capture/01-03-SUMMARY.md`
</output>
