---
phase: 01-ingestion-baseline-and-raw-capture
plan: 02
type: execute
wave: 1
depends_on:
  - "01-01"
files_modified:
  - src/ingestion/fetch_nasa_neos.py
  - src/ingestion/publish_stream.py
autonomous: true
requirements:
  - ING-01
  - ING-02
  - ING-03
user_setup: []
must_haves:
  truths:
    - Producer fetches NASA close-approach records on interval and handles rate-limit retries.
    - Producer publishes payloads to redis stream neo:stream with deterministic event id fields.
  artifacts:
    - src/ingestion/fetch_nasa_neos.py
    - src/ingestion/publish_stream.py
  key_links:
    - publish_stream imports fetch_nasa_neos fetch routine and shared runtime config.
---

<objective>
Implement resilient NASA API producer and stream publisher for raw event ingestion.

Purpose: Ensure Phase 1 has reliable data entry into Redis for downstream storage.
Output: Fetcher and publisher scripts with retry/backoff and stream write semantics.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/common/config.py
@src/common/logging_utils.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build NASA close-approach fetch client with retries</name>
  <files>src/ingestion/fetch_nasa_neos.py</files>
  <read_first>src/common/config.py, .planning/REQUIREMENTS.md</read_first>
  <action>Implement fetch_close_approach_records(start_date, end_date) using requests.Session and NASA close-approach endpoint from config. Add retry loop for HTTP 429 and 5xx using exponential backoff with jitter and max attempts = 5. Normalize response into list of dict objects containing object_id, object_name, close_approach_date, relative_velocity_km_s, miss_distance_au, estimated_diameter_m, source_payload, fetched_at_utc.</action>
  <verify>grep -E "def fetch_close_approach_records|max_attempts|429|miss_distance_au|estimated_diameter_m" src/ingestion/fetch_nasa_neos.py</verify>
  <acceptance_criteria>
    - fetch_nasa_neos.py defines fetch_close_approach_records(start_date, end_date)
    - retry logic handles 429 and 5xx with exponential backoff
    - normalized record contains miss_distance_au and estimated_diameter_m fields
  </acceptance_criteria>
  <done>Fetcher can pull and normalize records with graceful retry behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Publish normalized events to Redis stream</name>
  <files>src/ingestion/publish_stream.py</files>
  <read_first>src/ingestion/fetch_nasa_neos.py, src/common/config.py</read_first>
  <action>Create publish_records_to_stream(records) using redis-py xadd to stream neo:stream. Include fields event_id, object_id, close_approach_date, payload_json, fetched_at_utc. Build event_id as {object_id}:{close_approach_date}:{fetched_at_utc_epoch}. Add CLI entrypoint to fetch current-day window and publish all records with counters for success/failure.</action>
  <verify>grep -E "xadd|neo:stream|event_id|if __name__ == '__main__'" src/ingestion/publish_stream.py</verify>
  <acceptance_criteria>
    - publish_stream.py writes events using Redis xadd to neo:stream
    - event_id format includes object_id and close_approach_date
    - script entrypoint logs total published and failed record counts
  </acceptance_criteria>
  <done>Producer writes stream events ready for raw storage consumption.</done>
</task>

<task type="auto">
  <name>Task 3: Add producer smoke verification command</name>
  <files>README.md</files>
  <read_first>README.md, src/ingestion/publish_stream.py</read_first>
  <action>Add a short "Phase 1 smoke check" section documenting exact command to run producer and inspect stream length with redis-cli XLEN neo:stream. Include expected output interpretation for non-zero stream length.</action>
  <verify>grep -E "Phase 1 smoke check|XLEN neo:stream" README.md</verify>
  <acceptance_criteria>
    - README.md contains section title Phase 1 smoke check
    - README.md includes redis-cli XLEN neo:stream command
    - README.md explains that stream length greater than 0 indicates publish success
  </acceptance_criteria>
  <done>Operator can quickly validate producer->stream behavior.</done>
</task>

</tasks>

<threat_model>
- Asset: API credentials, data integrity, and stream availability.
- Threat: Retry storms on repeated API failure overwhelm resources.
- Mitigation: Max retry cap, bounded polling interval, and failure logging with controlled exits.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] Producer script exits successfully for a valid date window
- [ ] Redis stream length increases after publish run
- [ ] Retry behavior logs attempt numbers on transient errors
</verification>

<success_criteria>
- NASA data can be fetched and normalized
- Redis stream receives event payloads from producer
- Documentation includes producer smoke-test steps
</success_criteria>

<output>
After completion, create `.planning/phases/01-ingestion-baseline-and-raw-capture/01-02-SUMMARY.md`
</output>
